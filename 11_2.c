/* Write code that performs two tasks: defines a structure named data that
 * contains one type int member and two type float members, and declares
 * an instance of type data named info. */

struct data {
	int i_mbr;
	float f_mbr1;
	float f_mbr2;
}

struct data info;

/* Continuing with excercise 2, how do you assign the value 100 to the 
 * integer member of the structure info? */

info.i_mbr = 100;

